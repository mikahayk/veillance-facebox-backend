<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Latest captures</title>
  </head>
  <body>

    <% doc.forEach(function(item){ %>
     <img src="<%= headers.host %>/images/<%= item.image_name %>" style="height: 300px; ">
    <% }); %>

  </body>
</html>
